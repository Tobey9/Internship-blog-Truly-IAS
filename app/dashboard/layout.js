export const metadata = {
  title: "Dashboard | Truly IAS",
  description: "Create and manage your blog posts from the dashboard.",
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
